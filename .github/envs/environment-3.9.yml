name: test-environment
channels:
  - conda-forge
dependencies:
  # required dependencies
  - python=3.9
  - dask
  - aiohttp
  - requests
  - pytest
  - pyarrow
  - awkward
  - dask-histogram
  - uproot
  - hist

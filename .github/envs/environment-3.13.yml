name: test-environment
channels:
  - conda-forge
dependencies:
  # required dependencies
  - python=3.13
  - dask >=2025.3.1,<2025.4.0
  - aiohttp
  - requests
  - pytest
  - pyarrow
  - awkward
  - dask-histogram
  - uproot
  - hist
